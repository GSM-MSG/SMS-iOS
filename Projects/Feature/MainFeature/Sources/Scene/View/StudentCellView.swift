import SwiftUI
import DesignSystem

struct StudentCellView: View {
    var body: some View {
        VStack(alignment: .leading) {
            SMSIcon(.profile, width: 101, height: 101)
        }
    }
}
